$('.popover-content').html("<%= j(render('popover_email', q: @quote)) %>")
$('.email-yes-button').click(function(e) {
  var url;
  e.preventDefault();
  url = $(this).attr('href');
  return $.post(url, {}, void 0, "script");
});